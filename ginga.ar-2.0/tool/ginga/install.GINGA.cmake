if(WIN32)
	POST_BUILD_INSTALL_DIRECTORY("${PRE_BUILD_BIN_DESTINATION}" "${EXTERNALS_PATH}/bin/lua" )
	POST_BUILD_INSTALL_DIRECTORY("${PRE_BUILD_BIN_DESTINATION}" "${EXTERNALS_PATH}/bin/socket" )
	POST_BUILD_INSTALL_DIRECTORY("${PRE_BUILD_BIN_DESTINATION}" "${EXTERNALS_PATH}/bin/mime")
endif(WIN32)